service ciseedyservice
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = seedy
    bind        = 0.0.0.0
    server      = /opt/ciseedy/ciseedy/ciseedy.py
    type        = UNLISTED
    port        = 31337
}
